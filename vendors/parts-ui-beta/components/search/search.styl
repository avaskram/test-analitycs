+theme(search)
  &__link
    padding $rem-xxs
    font-size 0
    svg
      text-base()
      color #fff
      opacity .5
      transition opacity .2s ease, color .2s ease

    &:hover
      svg
        opacity 1
